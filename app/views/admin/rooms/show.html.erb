<p style="float:left"><%= link_to '← назад', admin_album_path(@photo.album) %></p>
<h1><%= if !@title.nil? then @title end %></h1>
<div class="clear"></div>

<% if @photo.image.exists? %>
<p style="text-align: center">
  <%= image_tag  @photo.image.url(:medium) %>
</p>
<% end %>